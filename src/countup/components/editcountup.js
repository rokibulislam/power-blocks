/**
 * Countup Wrapper
 */

// Setup the block
const { Component } = wp.element;

export default class editCountup extends Component {
	render() {
		return (
			<div>
				Coutup Admin
			</div>
		);
	}
}
