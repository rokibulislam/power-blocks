/**
 * Countup Wrapper
 */

// Setup the block
const { Component } = wp.element;
/**
 * Create a Countup wrapper Component
 */
export default class Countup extends Component {
	render() {
		return (
			<div>
				Countup Front
			</div>
		);
	}
}
